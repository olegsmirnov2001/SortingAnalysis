#include "TXLib.h"
#include "MyLib.h"

int main ()
    {
    CreateMyWindow ();

    while (!Exit ())
        {
        array_t <dyn, char> Arr (10);

        Arr.Move (200);

        txSleep (10);
        }

    _txExit = true;
    return 0;
    }
